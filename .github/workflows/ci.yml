name: CI

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        go-version: [1.22]

    steps:
    - uses: actions/checkout@v4

    - name: Set up Go
      uses: actions/setup-go@v4
      with:
        go-version: ${{ matrix.go-version }}

    - name: Cache Go modules
      uses: actions/cache@v3
      with:
        path: |
          ~/.cache/go-build
          ~/go/pkg/mod
        key: ${{ runner.os }}-go-${{ hashFiles('**/go.sum') }}
        restore-keys: |
          ${{ runner.os }}-go-

    - name: Download dependencies
      run: go mod download

    - name: Run tests
      run: make test

    - name: Run build
      run: make build

  build:
    runs-on: ubuntu-latest
    needs: test
    steps:
    - uses: actions/checkout@v4

    - name: Set up Go
      uses: actions/setup-go@v4
      with:
        go-version: 1.22

    - name: Build for Darwin ARM64
      run: |
        GOOS=darwin GOARCH=arm64 CGO_ENABLED=0 go build -o k8s-tui-darwin-arm64 -v ./cmd/k8s-tui

    - name: Build for Linux AMD64
      run: |
        GOOS=linux GOARCH=amd64 CGO_ENABLED=0 go build -o k8s-tui-linux-amd64 -v ./cmd/k8s-tui

    - name: Upload artifacts
      uses: actions/upload-artifact@v3
      with:
        name: binaries
        path: |
          k8s-tui-darwin-arm64
          k8s-tui-linux-amd64
